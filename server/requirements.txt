fastapi==0.115.0
uvicorn[standard]==0.31.0
onnxruntime==1.19.2
tokenizers==0.20.0
pydantic==2.9.2
numpy==1.26.4
# Stage 3 ITN: NeMo's WFST-based inverse normalizer. Pinned pynini
# version below its nemo_text_processing==1.1.0 strict pin because the
# pinned 2.1.6.post1 fails to build against openfst 1.8.4 (upstream API
# rename StringJoin → StrJoin). 2.1.7 builds cleanly and runs fine with
# nemo_text_processing. Install with `pip install -r requirements.txt
# --no-deps nemo_text_processing` or see Dockerfile for the full recipe.
pynini==2.1.7
nemo_text_processing==1.1.0
